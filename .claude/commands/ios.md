Run via Bash: `python scripts/skill_router.py skills/run/ios/SKILL.md`
If the script exits non-zero (LM Studio unavailable or model not loaded), fall back to reading `skills/run/ios/SKILL.md` and executing it yourself.
