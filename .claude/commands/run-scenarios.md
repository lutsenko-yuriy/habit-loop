Run via Bash: `python3 scripts/skill_router skills/run/run-scenarios/SKILL.md --args '$ARGUMENTS'`
If the script exits non-zero (LM Studio unavailable or model not loaded), fall back to reading `skills/run/run-scenarios/SKILL.md` and executing it yourself.

$ARGUMENTS
