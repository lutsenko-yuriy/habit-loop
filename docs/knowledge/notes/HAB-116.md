# HAB-116: Pact timeline screen

## Notes (captured mid-ticket in Linear)

- 2026-06-21: `audit-code` was not auto-invoked alongside `review-architecture` after WU1 PR — user had to invoke manually. WU2 worked correctly; likely a session context-compaction issue, not a structural skill gap.
- 2026-06-21: Plan said "seven milestone kinds" but named only six. 7th was `SingleShowupMilestone` (tail-zone, always tappable, carries `showupId`). Discrepancy was discovered in review; should be caught at plan time.
- 2026-06-22: `/debrief` skill was invoked prematurely during WU1 when the user asked to "write a comment for the debrief". Correct response: post a plain Linear comment. The skill is for the final retrospective after all WUs ship.
- 2026-06-22: When a user questions a code decision, explain the rationale and tradeoff first before applying the change — don't apply immediately without context.
- 2026-06-22: Dart-specific idioms missed in WU2 PR: initializer-list defaulting for cross-parameter defaults (eliminates nullable field); constructor params for configuration objects instead of method args. Check for Dart idioms before opening a PR.
- 2026-06-22: Pagination incompatible with the grouping algorithm — grouper needs the full showup list to compute tail zones and mixed groups. `loadPage()` API had to be redesigned mid-WU3 to `loadAll()`; windowing moved to the ViewModel. The planning phase did not catch this.
- 2026-06-22: Integration scenarios could not be made green by WU2–WU4 because the timeline UI (entry point + screen) didn't exist yet. Plan table said "made green WU2" — should say "filled in WU6". Verify UI existence before marking scenarios as exercisable in the plan.
- 2026-06-22: Cache eviction decision: `PactTimelineCache.evict()` placed in ViewModel, not `PactStatsService`. Rationale: stats service shouldn't need to know a timeline cache exists; coordinator (VM) is transparent about what it invalidates.
- 2026-06-23: Docs drift in WU5 — CHANGELOG still described the windowed approach after windowing was removed. Update docs in the same TDD cycle as the code, not as a follow-up commit.
- 2026-06-23: Large number of mid-ticket debrief notes raises overfitting risk — model may over-apply highly specific corrections. Consider keeping in-session notes lighter and letting the end-of-ticket debrief be the sole distillation point.
- 2026-06-23: `/note` skill vs plain Linear comment — the skill adds harness surface area; the same result could come from a plain comment with a template in AGENTS.md. *(Resolved later: `/note` skill was built in HAB-130 and proved its value.)*
- 2026-06-23: `/run-scenarios` output is opaque — no per-scenario progress during a long suite run. `--reporter expanded` prints test names but output isn't streamed live. Consider polling the output file.
- 2026-06-24: Misread background task output file as empty — `Read` with `offset: 0` returned "file shorter than offset" and 0 lines, incorrectly interpreted as still running. Use `cat` via Bash to read background task output; wait for the completion notification rather than polling.
- 2026-06-24: Firebase App Distribution received partial builds mid-ticket (iOS-only after WU6). Proposal: suppress distribution during multi-WU tickets and distribute only when the ticket reaches In QA. *(Resolved by `[wip]` CHANGELOG tag introduced in the debrief.)*
- 2026-06-24: Per-incident Linear comments scatter context. Proposal: maintain a local `docs/knowledge/notes/HAB-XX.md` file during implementation and upload at the end. *(Resolved by `/note` skill from HAB-130.)*
- 2026-06-24: WU7 (Android) shipped clean on the first pass — iOS work (WU6–WU6.2) absorbed all design iteration. Takeaway: for multi-platform features, delivering iOS first and iterating to completion before starting Android means the Android WU is a clean port, not a discovery exercise.

## Debrief summary

### 2026-06-25

**What went well**
- Substantial UX feature shipped across iOS and Android; spine layout and golden-ratio proportions landed well
- Android WU7 was a zero-complaint, zero-failure pass — benefit of completing iOS design iteration first
- Cache eviction architecture discussion was productive and landed on the cleaner design

**What was hard or surprising**
- Pagination vs grouping algorithm incompatibility — the plan committed to a paged service API before verifying the algorithm requires a full pass; had to redesign mid-WU3
- Visual design happened late (WU6→WU6.2 iteration) rather than upfront; significant rework in review
- Integration scenarios couldn't be made green until WU6 because the UI entry point didn't exist — plan language was misleading

**What was changed (shipped in PR #178)**
- `brief` skill: added visual alignment step — asks for a reference/metaphor and produces an ASCII mockup before creating the ticket; reduces mid-ticket redesign
- `plan` skill: scenario timing language now distinguishes "filled in WUN" (scenario exists; UI needed) from "made green WUN" (fully exercisable); removes ambiguity
- Workflow: planning phase now explicitly checks algorithm/pagination compatibility before committing to a paged API
- `[wip]` CHANGELOG tag: intermediate WU merges suppress builds and distribution; the final WU uses `[user]`/`[app]`; "What's New" accumulation is unaffected
