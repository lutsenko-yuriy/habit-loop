# HAB-126: Merge PactTimelineCache and PactStatsCache into a single cache

## Notes

- 2026-06-28: Ticket was written some time ago and lacked crucial context at the point development started — specifically, what the expected implementation approach was and what had changed in the codebase since the ticket was filed (e.g. `syncStats` bypassing the shared cache, the single-loop stretch goal). Next time a cached ticket is picked up, review it against the current code before starting to check if the description still matches reality.
- 2026-06-28: /note skill confirmation question is redundant when there is an active ticket — just state "Noting to HAB-XX (date)" and write immediately without waiting for a reply.

- 2026-06-29: Debrief — share architectural ideas earlier. The PR was built without knowing Yuriy had a different architectural vision (caching a single pact's data in one loop). That mismatch burned tokens and time. In future, ask the user if they have architectural ideas they'd like to share before planning or implementing anything cache/performance-related.
- 2026-06-29: Debrief — need performance measurement infrastructure. Before optimising the pact detail/stats/timeline section, establish a way to measure actual performance (Flutter profiler + existing logs). Without baseline numbers, it's impossible to know whether a refactor helps or hurts.

## Debrief summary

### 2026-06-29

**What went well**
- Nothing notable — ticket was cancelled before producing value.

**What was hard or surprising**
- The user had an architectural idea (single-loop stats + timeline generation) before the ticket was even briefed, but never surfaced it. By the time it became clear the idea wouldn't work, implementation was already underway.
- Even after realising the idea was unworkable, the user allowed implementation to continue, burning further tokens.

**What to change**
- Plan skill now asks about architectural intent before reading the codebase (step 1.5).
- Plan skill now gates on potentially unworkable intent: surfaces the concern and asks whether to proceed with a different approach, proceed anyway, or abandon — before producing a plan.
- Created HAB-138 to research Flutter performance measurement techniques, so future performance-related work has a baseline to reason from.
