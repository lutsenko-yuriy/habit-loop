# HAB-154: Standing docs audit and tidy-up

## Notes

### 2026-07-11

**Writing feedback + review-architecture caught a real bug**

Kudos from the user, worth preserving verbatim in spirit: the rewritten `dialog-principles.md`/`decision-principles.md` wording turned the user's own loosely-stated intent into a cleaner, more understandable statement than they'd been able to phrase themselves. Separately, `/review-architecture` on PR #251 caught a real defect before approval — `@skills/shared/decision-principles.md` had been inserted inside `experiment/SKILL.md`'s YAML frontmatter (between `description:` and the closing `---` fence) instead of after it, which would have broken that skill's frontmatter parsing. Confirms the review skill is pulling its weight even on docs-only PRs where the usual layer/dependency checks don't apply.

**This very note got orphaned before it shipped**

This file's first entry was written by `/note` mid-session, but never `git add`ed — `/ship` only staged `docs/CHANGELOG.md` and `pubspec.yaml`, so the note sat untracked through the merge and survived the branch switch to `chore/calibrate-model-tiers` (untracked files aren't tied to a branch). It only surfaced when checking `git status` before an unrelated commit, well after HAB-154 was already Done. Fixed by folding it into PR #252 instead of opening a dedicated fix PR, at the user's request. Worth raising at debrief: `/ship` step 6 stages an explicit file list, so any `/note`-created file from earlier in the same session is silently excluded unless something prompts a `git status` check first.

## Debrief summary

### YYYY-MM-DD

**What went well**
- …

**What was hard or surprising**
- …

**What to change**
- …
