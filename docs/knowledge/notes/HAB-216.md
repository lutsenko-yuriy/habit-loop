---
bookmarks: [debugging-methodology, ci-flakiness]
---

# HAB-216: Merge break runs into a single timeline milestone, tail zone included

## Notes

### 2026-08-09

**Guideline: timeline grouping stays single-pass**

`PactTimelineGrouper.groupWithStats` must stay a single forward pass over showups. The HAB-216 plan's three fixes (tail-branch break handling, cross-branch run accumulation, `coveringBreak` computed for resolved showups too) all land inside the existing single pass, with `tailStartIndex` resolved lazily at flush time (via a `touchesTail` flag on the break-run accumulator) rather than via a second pass. Keep this invariant for any future grouper changes.

## WU scope summary (plan approved 2026-08-09)

- **WU0** — Integration scenarios (S1–S5) for the merged-break-milestone behaviours, committed `[test]` before any production code. `integration_test/break_timeline_flow_test.dart` + `test_runner.dart` registration.
- **WU1** — `BreakMilestone` (new sealed-union member in `pact_timeline_milestone.dart`) + `PactTimelineGrouper` restructure (break-run accumulator, lazy `tailStartIndex`, `coveringBreak` for resolved showups too) + matching switch arms on both platform pages (`pact_timeline_page_ios.dart`, `pact_timeline_page_android.dart`) + formatters. Old `isOnBreak`/`breakRationale` fields left in place (dead but compiling) so this WU is additive only. All scenarios go green here. `[wip]`.
- **WU2** — Delete the now-dead `isOnBreak`/`breakRationale` fields and their UI branches; update `docs/ARCHITECTURE.md` and the `docs/ANALYTICS_EVENTS.md` note about tail-zone taps no longer firing `single_showup`. Net-negative LoC. Final WU — `[user]` (user-visible: a break always renders as one timeline entry).

## Debrief summary

### 2026-08-10

**What went well**
- Smooth end-to-end for a 2-WU ticket touching a core grouping algorithm — no significant issues surfaced.
- The single-pass guideline captured mid-WU1 held: the final grouper restructure landed inside the existing forward pass, no second pass needed.
- WU2's audit findings (stale `BreakDerivation` doc claim, narrow `pact_timeline_milestone_tapped` note, undocumented `milestone_count` metric discontinuity) were all cheap, low-risk doc fixes — applied immediately rather than deferred.

**What was hard or surprising**
- Nothing significant — the ticket's biggest friction was CI-only timing/finder issues in the WU1 scenario driver code (below), not the production logic itself.

**What to change**
- Cap re-dispatch attempts on `scenarios.yml` at 3 for scenarios *outside* the PR's own scope (pre-existing flakiness unrelated to the diff) — postpone/track separately after 3 reds rather than re-dispatching indefinitely. Scenarios the PR itself must make/keep green are exempt from the cap. WU1's review loop hit known `pact_note_flow_test.dart` flakiness on 3 of 5 dispatches — acceptable this time given the ticket's size, but nothing bounded it. Applied to `docs/FEATURE_WORKFLOW.md` step 10.6.
- Tell the user upfront when a CI-dispatch loop will likely take several cycles, so they know it's safe to step away — `docs/TROUBLESHOOT_WORKFLOW.md` already asks for this in the bug-fix flow, but `FEATURE_WORKFLOW.md`'s own review-loop step 10.6 had no equivalent framing. Added.
- Two wrong fixes before landing S4's stats-scroll assertion (backwards scroll direction, then a `dragUntilVisible(...).first` crash on an empty finder) — both were reasoned from first principles rather than from a live CI diagnostic each time; re-reading `docs/TROUBLESHOOT_WORKFLOW.md`'s "add targeted diagnostic instrumentation" guidance sooner would likely have shortened this.
