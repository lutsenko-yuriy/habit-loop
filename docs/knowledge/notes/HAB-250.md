---
bookmarks: [review-findings, knowledge-base-process]
---

# HAB-250: Bug: notes/index.py picks up untracked note files, causing recurring stale-INDEX CI failures

## Notes

## Debrief summary

### 2026-08-23

**What went well**
- The three-round review loop (architecture + audit-code, each re-dispatched after fixes) earned its keep on a small, mechanical-looking fix: each round caught a real issue the previous fix introduced or missed — round 1 found the core bug and the root fix; round 2 found the `HAB-XX-adjust-N.md` fallback path wasn't covered by the new staging instruction, plus that `/note` had no commit step of its own (a staged-but-uncommitted note could get swept into an unrelated commit); round 3 found the regenerated `INDEX.md` itself was left unstaged after `/note`'s new commit step, and that the commit had no pathspec (risking sweeping in unrelated staged work). All fixed and verified live against the exact real-world repro (an actual untracked note file sitting in the working tree) before merging.
- Live reproduction throughout — every fix was checked against `docs/knowledge/notes/HAB-247.md` (a genuinely untracked file already present from earlier in the session) rather than only against the new unit tests.

**What was hard or surprising**
- None raised.

**What to change**
- Nothing for this ticket specifically. Two adjacent, out-of-scope observations were noted on the Linear ticket for possible future tickets: `checkup`'s heavy-tier re-bookmarking pass doesn't regenerate `INDEX.md` afterward (same failure shape, different trigger), and `/note` doesn't check the current branch before its new commit step.
