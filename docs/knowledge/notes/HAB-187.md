# HAB-187: Design-system foundation + accessibility pass

## Notes

### 2026-07-20

**Checkup findings may need their own triage workflow**

The 2026-07-20 heavy checkup produced 10 open ledger findings. Before turning them into tickets, we did an ad-hoc pass to (1) find real dependency/impact relationships between findings (not just "same dimension") and draw a directed graph, (2) separate the connected cluster from genuinely independent findings, and (3) decide how to group them into tickets (HAB-187 for the connected cluster, HAB-188 for the independent ones) rather than filing 10 separate tickets or forcing everything into one. None of this triage step exists in `docs/RESEARCH_WORKFLOW.md`, `docs/FEATURE_WORKFLOW.md`, or the `/checkup` skill itself — it was improvised in conversation. Worth raising at debrief: should the checkup process (or a follow-up step after it) formalize a "triage the ledger into tickets" workflow, including the dependency-graph step, instead of leaving it to be reinvented each time a batch of findings is ready to be scheduled?

**Testing rigor for checkup-sourced tickets**

Every finding rolled into HAB-187 (and HAB-188) was originally deferred specifically *because* it touches production logic and needs its own test-writing cycle (per the Boy Scout Rule in `skills/manage/checkup/resources/findings-protocol.md`) — that's the whole reason they weren't fixed inline during the checkup. When implementing these tickets, TDD rigor is not optional/nice-to-have — it's the specific justification for why this work was deferred out of the checkup in the first place. This applies with extra weight to the accessibility items (heavy-8, heavy-9): correctness there should be verified with `flutter_test`'s `meetsGuideline` matchers (tap-target, contrast), not just visual inspection, since those findings exist because the app had *zero* accessibility test coverage to begin with.

### 2026-07-20 — WU scope summary (plan approved)

Full plan posted as a Linear comment on HAB-187. Per-WU scope, for resuming after context compaction:

- **WU0** — `feature/HAB-187-WU0-scenarios`. No new integration scenarios (pure tech-debt/accessibility, no new user flow — `draft-scenarios` skipped per FEATURE_WORKFLOW's infra/refactor exemption). Audit only: confirm the existing suite (`create_pact_flow`, `edit_pact_flow`, `archive_pact_flow`, `stop_pact_flow`, `dashboard_kebab_menu_flow`) is green as the regression baseline before touching anything.
- **WU1** — `feature/HAB-187-WU1-pacts-panel-split`. Split `_PactsPanelState.build` (`lib/slices/pact/ui/generic/pacts_summary_bar.dart`) into 4 sub-widgets. Covers `CHK-2026-07-20-heavy-2`. ~220 LoC.
- **WU2** — `feature/HAB-187-WU2-cupertino-picker-sheet`. Extract `showCupertinoPickerSheet` (`lib/slices/pact/ui/ios/cupertino_picker_sheet.dart`, feature-local) from the 5 duplicated scaffolds in `schedule_step_ios.dart`. Covers `CHK-2026-07-20-heavy-3`. ~150 LoC net −.
- **WU3** — `feature/HAB-187-WU3-design-tokens`. `AppSpacing`/`AppTypography` as bare `abstract final class` constants (`lib/theme/spacing.dart`, `lib/theme/typography.dart`), adopted in the WU1/WU2 widgets first. Covers `CHK-2026-07-20-heavy-7`. ~180 LoC.
- **WU4** — `feature/HAB-187-WU4-dashboard-a11y`. Dashboard + app-bar accessibility: semantic labels/tooltips, tap-target sizing, day-circle `FittedBox`. Covers `CHK-2026-07-20-heavy-8` (dashboard portion) + `CHK-2026-07-20-heavy-9` (day-circle text-scaling — same region as the tap-target fix). New l10n keys in all 4 arb files. ~230 LoC.
- **WU5** — `feature/HAB-187-WU5-wizard-a11y`. Wizard/pact-creation accessibility: step indicator, slot editor, option tile, remove buttons. Covers `CHK-2026-07-20-heavy-8` (wizard portion). New l10n keys in all 4 arb files. ~260 LoC.
- **WU6** — `feature/HAB-187-WU6-contrast`. WCAG AA contrast pass: `CupertinoColors.systemGrey` → AA token, `DateRowTile` default `valueColor`. Covers the remainder of `CHK-2026-07-20-heavy-9`. Final WU — debrief runs here, before ship. ~200 LoC.

Test strategy hard requirement (per the earlier note above): WU4–WU6 use `flutter_test`'s `meetsGuideline(androidTapTargetGuideline/iOSTapTargetGuideline/textContrastGuideline)` and `matchesSemantics(...)` as real assertions, not visual checks only.

### 2026-07-21 — plan revision: new WUs 7–10 (spacing widen + token-adoption sweep)

Triggered while reviewing PR #304 (WU3): a whole-`lib/` audit of `EdgeInsets`/`SizedBox` literals showed the real spacing rhythm is denser than #304's 5-step named scale. Full revised plan posted as a Linear comment on HAB-187. New per-WU scope, for resuming after compaction:

- **WU7** — `feature/HAB-187-WU7-spacing-numeric-rename`. Switch `AppSpacing` from named (`xs/sm/md/lg/xl`) to numeric `s`-prefixed tokens (`s4/s8/s12/s16/s24`) and widen with `s2/s6/s10/s14/s20/s32` (every value recurring ≥3 in the audit). Atomically update the 7 files #304 migrated (~41 call sites: 6 iOS `*_step_ios.dart` + `pacts_summary_bar.dart`), `spacing_test`, and the now-stale `docs/ARCHITECTURE.md:59` naming reference. `[wip]`. ~130 LoC.
- **WU8** — `feature/HAB-187-WU8-spacing-sweep`. Migrate the remaining ~250 raw `EdgeInsets`/`SizedBox` literals across `lib/` (~45 files) onto the widened tokens. Snap one-offs `7→s8` (2 sites) and `5→s6` (1 site); **leave `1` raw** (`showup_status_dots.dart:62` horizontal:1 — snapping to s2 doubles inter-dot margin, visible) and `28` raw. Behaviour-preserving. `[wip]`. Deliberately exceeds the ≤10-file cap (uniform mechanical substitution, near-zero net LoC); can be sharded by slice if the reviewer prefers.
- **WU9** — `feature/HAB-187-WU9-color-adoption`. Adopt `HabitLoopColors` for the small set of raw brand-colour literals. **Do NOT migrate adaptive `CupertinoColors` semantics** (`secondaryLabel`/`separator`/`systemBackground`/`tertiarySystemFill` — static brand colours would break dark mode). Does not touch `systemGrey` (WU6). `[wip]`. ~80 LoC.
- **WU10** — `feature/HAB-187-WU10-typography-adoption`. Expand `AppTypography` with named styles for recurring `TextStyle` patterns; migrate `TextStyle`/`fontSize` literals (87 `TextStyle` sites total). Preserve existing rendered colours (contrast fixes stay WU6). Split iOS vs Android/generic if >300 LoC. `[wip]`. ~250 LoC.

Recommended execution order after #304 merges: **WU7 → WU8 → WU4 → WU5 → WU9 → WU10 → WU6** (WU6 stays final; debrief there). Rationale: WU7 renames #304's just-shipped tokens so it must precede any later adoption; WU8 before the a11y WUs avoids re-migrating new padding; WU9/WU10 before WU6 so contrast fixes land on centralised tokens. **PR #304 recommendation: keep as-is, do the rename in WU7** (folding it in would invalidate #304's review and mix charters).

### 2026-07-21 — idea: CI/lint check for magic spacing numbers

While reviewing WU8 (PR #306), user asked whether a lint/CI check could catch raw numeric literals used in `EdgeInsets`/`SizedBox`/`Padding` instead of `AppSpacing` tokens, without rewriting the widget system. Out of scope for HAB-187 (token adoption is a one-time sweep, not an enforcement mechanism) — two options discussed:

1. **Lightweight regex/text-scan script** (recommended starting point) — mirrors `scripts/changelog/lint.py`'s existing pattern: scan `lib/` for `EdgeInsets`/`SizedBox`/`Padding` constructor calls containing a bare numeric literal, fail CI unless allowlisted (e.g. the `showup_status_dots.dart:62` exception). No new dependency, plugs into the existing `test.yml`/`run-tests` composite.
2. **`custom_lint` package** — real AST-aware analyzer plugin rule, no false positives from strings/comments, shows inline in IDE like a normal lint. Repo currently only has `flutter_lints`, not `custom_lint` — bigger one-time setup cost.

Filed as a follow-up ticket rather than folded into HAB-187 — see the ticket for tracking.

### 2026-07-20 — WU0 baseline result

Ran the full integration suite (`integration_test/test_runner.dart`) on a booted iPhone 17 Pro Simulator (the wireless physical iPhone can't run it without `--publish-port` — infra note, not a code issue). 63/64 passed. The one failure, `pact_timeline_flow_test.dart` `status_write_through_visible_in_timeline_same_session` (mark-done via Showup Detail reflected in Timeline), failed with a hit-test-offset warning on the "Mark as Done" tap followed by a `waitFor` timeout — but passed cleanly when the file was re-run in isolation. Confirmed flaky under full-suite load, not a real regression, and unrelated to anything HAB-187 touches. Likely the iOS-simulator counterpart of the Android emulator timing flakiness HAB-179 already fixed. Not fixing here — out of scope for this ticket — but worth a future troubleshooting ticket if it recurs. Treating the baseline as green; proceeding to WU1.

### 2026-07-22

**Deep widget-tree nesting is the widget-layer analogue of the control-flow nesting rule**

PR #307 (WU4) review flagged `_CalendarStrip`'s day-cell subtree as too deeply nested (`Semantics` → `GestureDetector` → `ExcludeSemantics` → `ConstrainedBox` → `Center` → `Column`, ~7 levels) and pointed at the WU1 precedent (`pacts_summary_bar.dart` god-widget split) as the fix. Extracted into `_CalendarDayCell`. `docs/CODE_STYLE.md`'s "Control flow" section already has a rule for `if`/`for`/`try` nesting past ~3 levels ("extract the inner block into a well-named function") but says nothing about widget-tree nesting specifically, even though the same readability cost applies and the same fix (extract into a named widget) resolves it. Consider adding an explicit widget-tree nesting guideline alongside the existing control-flow table at debrief.

## Debrief summary
