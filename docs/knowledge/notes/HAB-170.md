# HAB-170: Migrate existing decision-like docs into the ADR structure (docs/decisions/)

## Notes

## Debrief summary

### 2026-07-15

**What went well**
- Extending the survey to `docs/knowledge/notes/*.md` (not just top-level `docs/*.md`) surfaced a second real candidate (HAB-140's redemption review-policy decision) that a top-level-only survey would have missed.
- The confirm-before-migrating gate worked as intended — no speculative migration, both candidates reviewed and approved before any file was written.

**What was hard or surprising**
- Expected more decision-like content to surface than the two candidates found. Concluded this is fine — the grep-based survey is inherently conservative (only catches ticket notes using explicit decision-framing language), and forcing ADR creation where there's no clear standing-decision shape isn't the goal.

**What to change**
- Nothing — no forced ADR creation process; the current judgment-based survey approach stays as-is.
