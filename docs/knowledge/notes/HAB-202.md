# HAB-202: Adjust and start again: create a new pact from a finished one, chained

## Notes

<!-- Each entry: dated heading, then named observations (bold title + paragraph).
     If the ticket is reopened after this file is finalised, create HAB-XX-adjust-N.md instead of editing this file. -->

### 2026-07-29

**Per-WU scope (from the approved plan)**

- **WU0 — scenarios + docs** (`[test]`). New `integration_test/pact_chain_test.dart` with drafted (red) chain scenarios, plus commit the already-drafted-on-`main` `docs/GLOSSARY.md` "Chain (of pacts)" and `docs/ANALYTICS_EVENTS.md` entries (they are uncommitted working-tree changes to fold in here, not re-create). Nothing green yet.
- **WU0-scenario_correction — navigation-stack scenarios** (`[test]`). Correction added before WU1 started: "Previous Pact"/"Next Pact" links must not let the user build up an unbounded back stack by bouncing between a predecessor (P_p) and successor (P_c) pact. Rule: each pushed `PactDetailScreen` remembers the pact id of whatever detail screen pushed it (`cameFromPactId`, null if opened from the list); tapping a chain link pops if the target matches `cameFromPactId`, otherwise pushes a new page. Two scenarios added to `pact_chain_test.dart`: `chain_navigation_previous_then_next_returns_to_same_page_instance` (P_c → Previous → P_p → Next → back to the same P_c instance) and `chain_navigation_next_then_previous_returns_to_same_page_instance` (P_p → Next → P_c → Previous → back to the same P_p instance) — each verified via a single `tester.pageBack()` landing directly on the Pacts List. This nav rule is WU3's to implement (`_onOpenChainLink` in `pact_detail_screen.dart`); WU4 still drives all chain scenarios green as before, WU3 renders the links and WU4 wires the "Adjust and start again" entry point per the original split.
- **WU1 — domain + persistence + repo enforcement** (`[wip]`). `Pact.predecessorPactId` (`String?`, immutable after insert) through `Pact`/`PactMapper` (`toRow`/`fromRow` only — not `toUpdateRow`)/`SyncMapper`; schema migration v6 (`ADD COLUMN predecessor_pact_id TEXT` + **partial unique index** `WHERE predecessor_pact_id IS NOT NULL` — this is what enforces "at most one successor"; **no self-ref FK**, to avoid Firestore pull-order violations); `PactRepository.getSuccessor(pactId)` + `savePact` successor guard in both `SqlitePactRepository` and `InMemoryPactRepository`. Touches domain + persistence + sync mapper — In QA (migration).
- **WU2 — chain application logic** (`[wip]`). New `PactChainService`: default habit name by walking to the root pact and counting ancestors → `"<root> (v{n+1})"`, and a prefilled `PactBuilder` (start date reset to today, other fields carried). `PactBuilder.build(predecessorPactId:)`, `createPactFromBuilder` plumbing, a creation-config provider, `PactCreationViewModel.build()` seeding + submit persisting the predecessor, and `PactCreatedEvent` enrichment (`source`/`predecessor_pact_id`/`*_changed_from_*`). Prefill is computed async in the caller and handed to the sync `build()` via the config provider (mirrors the edit flow). No UI entry yet.
- **WU3 — detail read-side + feature toggle** (`[wip]`). Load predecessor + successor into `PactDetailState`/ViewModel; render "Previous Pact" (under name/badge) and "Next Pact" (bottom area) links on both platforms with navigation; `PactChainLinkTappedEvent`; new Remote Config kill-switch `pact_chaining_enabled` (default `true`) gating button + links, documented in `docs/FEATURE_TOGGLES.md`; l10n. Link rendering covered by widget tests.
- **WU4 — "Adjust and start again" action** (final WU → `[user]`). Button on finished pacts (both platforms) in the bottom action spot, `_onAdjustAndStartAgain` (build prefill via `PactChainService`, seed config, push `PactCreationScreen`, reload on return), `PactAdjustAndStartAgainTappedEvent`, `PactWizardAbandonedEvent` `source`. Drives all end-to-end chain scenarios green.

**Cross-WU invariant reminders**

- "At most one successor" is enforced at the data layer (partial unique index + `savePact` guard), not just by hiding the button — per the approved architectural decision.
- The version-number default name is computed once at creation from the chain root, never recomputed; renaming any pact later does not retroactively change others.
- No stats/streak carryover — the chain is purely navigational/informational.

**Open risk — concurrent-creation sync conflict (from WU1 audit, PR #338)**

If two offline devices each create a successor to the same predecessor, the `savePact` guard/unique index correctly reject the second one locally — but on Firestore pull-merge, the rejection is silently swallowed by the per-record try/catch (`firestore_sync_service.dart` `_mergeRemotePact`, pull loop). The skipped record never becomes locally dirty, so it's never retried: one successor permanently never lands on other devices, even though it still exists in Firestore and on its origin device. Not reachable until the chaining UI ships (no blocker for WU1/WU2), but needs a conflict-resolution rule designed **before WU3**. Leaning toward last-write-wins as the simplest option — see the product-direction note below on why a more elaborate rule likely isn't worth designing yet.

**Product-direction note (2026-07-29): possible future generalization to a directed graph**

Discussed while triaging the above: the current ticket explicitly scopes the chain relationship as strictly linear (at most one successor — see "Out of scope" in the ticket body), but there's a standing intent to possibly generalize this into a directed graph (multiple successors / branching) in a later ticket. This is **not** in scope for HAB-202 and nothing here should be built to anticipate it — flagging only so the WU3 conflict-resolution design (and any other one-successor-shaped decision) is made with "cheapest reasonable option, likely to be discarded on a future graph generalization" in mind rather than over-engineered now.

## Process observations (for debrief)

- **Mid-implementation scope corrections that touch scenarios get their own inserted WU, named `WUN-<short-correction-slug>` (not a decimal like `WUN.1`), not folded into the current or the next WU.** `WU0-scenario_correction` (navigation-stack scenarios, added before WU1 started) is the first instance. Generalized rule: if a correction surfaces *during* WUN's implementation and it changes what a scenario should cover, the fix lands in a new `WUN-<short-correction-slug>` branch/PR (e.g. `WU2-scenario_correction`) between WUN and WU(N+1) — never retrofitted into WUN's own commits (which may already be reviewed/merged) and never deferred into WU(N+1) (which has its own unrelated scope). Candidate workflow update: propose adding this as an explicit rule in the "Multi-WU tickets" appendix of `docs/FEATURE_WORKFLOW.md` at debrief time.

## Debrief summary

<!-- Filled at /debrief after the final WU (WU4) ships. -->
