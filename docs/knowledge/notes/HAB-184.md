---
bookmarks: [review-findings]
---

# HAB-184: Fix false positives in dead-code-check's orphaned test-file detector

## Notes

## Debrief summary

### 2026-08-16

**What went well**
- Fully-specified ticket (exact behaviour, root cause, out-of-scope already written) meant no `/plan` pass was needed — straight to TDD.
- The audit loop earned its keep again: it caught two real bugs in the fix itself — unbounded substring containment and basename-only import matching — both of which were silent *under*-reports, the same failure class as the ticket's original *over*-report, just inverted. Both fixed with regression tests before merge.
- Widened the ticket's stale `.github/workflows/ci.yml` reference to the whole `.github/` tree instead of hardcoding a path that had already drifted (renamed in HAB-183) — avoids the same kind of staleness this ticket was fixing.

**What was hard or surprising**
- Nothing new.

**What to change**
- Nothing new.
