# HAB-188: Checkup cleanup: dashboard load method, Firestore merge dedup, Android buttons, iOS toolbar icon, pagination stub

## Notes

<!-- Each entry: dated heading, then named observations (bold title + paragraph).
     If the ticket is reopened after this file is finalised, create HAB-XX-adjust-N.md instead of editing this file. -->

### 2026-07-23

**WU scope summary (5 independent WUs, plan approved as-is)**

- **WU1 — Delete dangling pagination test stub.** Touches only `integration_test/pact_timeline_flow_test.dart` (deletes the `group('Pact timeline — pagination', ...)` block, lines 411-427). Product decision already recorded (Linear comment on HAB-188): pagination was fully dropped from `PactTimelineService`/`PactTimelineConfig` in HAB-116, so the 6 unfilled `// TODO:` steps are dead scaffolding, not a feature to build. `[test]` tag.
- **WU2 — Android primary-button hierarchy.** Touches `dashboard_page_android.dart` (`buildNoPactsCta`) and `pact_detail_page_android.dart` (`buildSaveButton`), plus their widget tests. Replaces two stray `ElevatedButton`s with `FilledButton` to match the hierarchy already consistent everywhere else in the Android UI (`FilledButton`=primary, `OutlinedButton`=secondary, `TextButton`=tertiary). User-visible but minor.
- **WU3 — iOS toolbar sync icon platform-awareness.** Touches `sync_status_handler.dart` (splits `syncStatusIconData` into `syncStatusIconDataMaterial`/`syncStatusIconDataCupertino`, mirroring the existing `ShowupStatusColors.material()`/`.cupertino()` pattern), `dashboard_page_ios.dart` (switches to the Cupertino variant), and `sync_status_ui_test.dart` (the existing iOS assertions at lines 138/145 currently assert Material `Icons.*` against the iOS page — that's the bug being fixed, so those two assertions need updating). New `sync_status_handler_test.dart`. User-visible but minor.
- **WU4 — `DashboardViewModel._loadInner` split.** Touches only `dashboard_view_model.dart`. Pure, behaviour-preserving refactor: splits the ~175-line method into named steps and extracts a shared `_autoFailShowup` helper to de-duplicate the gap-fill-sweep/auto-fail-sweep try/catch-log-analytics-cancel-increment shape. No test changes — the existing 1259-line `dashboard_view_model_test.dart` suite is the regression gate (must stay green, unmodified).
- **WU5 — Firestore merge-rule dedup (final WU).** Touches only `firestore_sync_service.dart`. Pure, behaviour-preserving refactor: unifies `_mergeRemotePact`/`_mergeRemoteShowup` behind one generic `_mergeRemoteRecord<T>` helper, with the showup-only TOCTOU re-check passed in as an optional `existsGuard` callback. No test changes — existing 984-line `firestore_sync_service_test.dart` suite is the regression gate.

**CHANGELOG tagging reminder for `ship` at WU5:** WU1-4 ship `[wip]` (no build/release notes). WU5's entry should be tagged `[user]` and its bullets should cover *all* of the ticket's user-visible fixes (WU2 button hierarchy + WU3 icon fix), not just WU5's own diff — otherwise those minor-but-real user-facing improvements are silently lost from "What's New" behind their own `[wip]`-tagged WUs. User approved this approach when approving the plan.

**ARCHITECTURE.md:** confirmed no update needed — every WU is an internal refactor within an existing file/class (private method splits, a generic private merge helper, a Material/Cupertino function split mirroring an already-documented pattern). No new layers, directories, public classes, or dependencies introduced.
