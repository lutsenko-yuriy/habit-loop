# HAB-141: Dashboard stale showup status after notification tap

## Notes

## Debrief summary

### 2026-06-30

**What went well**
- User provided a detailed, reproduction-ready bug description from the start (exact steps, expected vs actual, key observation that it works from the todo list but not from notification)
- Three clarifying questions were asked and all three landed well: (1) does reopening the app fix it — confirmed data is persisted correctly; (2) does the same happen on mark-as-failed — confirmed it's not status-specific; (3) reproducibility — confirming consistent behaviour. Each question narrowed the root cause without redundancy.
- Root cause was identified immediately from the code (`unawaited(state.push(...))` in `NotificationNavigator` vs the awaited path in `onShowupTapped`), and the fix was small and precise.
- Two integration test scenarios were written covering both done and failed paths via the notification navigation route.
- Positive example: brief description → focused clarifying questions → immediate fix → tests → shipped in one session.

**What was hard or surprising**
- Nothing unexpected. The fix was straightforward once the navigation paths were compared.

**What to change**
- Nothing. This ticket is a reference example of the expected workflow: detailed report → minimal clarifying questions → fix → test coverage → ship.
