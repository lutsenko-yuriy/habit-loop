---
bookmarks: [debugging-methodology, postmortem-workflow]
---

# HAB-209: Postmortem: find when/why stop-pact started deleting ALL showups

## Notes

### 2026-08-16

**Origin: PR #21 (commit 3728387, 2026-04-18) — deliberate design, not an accident**

`git log -S"deleteShowupsForPact"` traces the behavior back past both HAB-11 WU3 (`afc0d35`, which only wrapped an *already-existing* two-step delete in a transaction) and HAB-11 WU4 (`75b2b56`, which only swapped the in-memory repo for SQLite) to the true origin: PR #21, "[codex] Fix pact stats persistence and dashboard analytics" (2026-04-18, authored via Codex — the one PR in this file's history not co-authored by a Claude model). `PactStatsService.stopPact` was introduced there already deleting all showups on stop, deliberately: the commit paired the deletion with a **frozen `PactStats` snapshot** written onto `pact.stats` *before* the delete, with an explicit comment: "Returns fresh stats whenever showups still exist, falling back to the persisted snapshot only after the showups were deleted (stopped pact)." Delete-all was never an oversight — the author designed a safety net for it up front.

**Detection gap: the safety net was silently amputated three weeks later, with nothing to notice**

HAB-11 WU2 (commit `541db06`, 2026-05-05, SQLite persistence) introduced `PactMapper.fromRow`/`toRow` and made a *separate*, seemingly reasonable call: `pact.stats` is "not persisted — always computed from SQL aggregates at read time," so `fromRow` always returns `stats: null`. That decision, on its own, looks fine for an *active* pact (stats recompute from its live showups). But it silently deletes the one thing PR #21's stopped-pact fallback depended on — nobody at WU2 time connected "stats null on every load" back to "the stopped-pact snapshot must survive a load or the fallback in `currentStats` is dead code." Neither PR's tests would have caught the interaction: HAB-11 WU3's `pact_transaction_service_test.dart` (which does assert "all showups deleted") builds its `Pact` objects directly in Dart and calls `stopPactTransaction` on them — it never round-trips through `PactMapper.fromRow`, so a test that faithfully encoded the delete-all *contract* had no way to see that the contract's own safety net was broken elsewhere. The gap wasn't a missing assertion in one test — it was two correct-in-isolation decisions, three weeks apart, with no test spanning both.

**Answering the ticket's item 3 directly:** yes, `stopPactTransaction`'s test asserting "all showups deleted" as expected/passing is real (confirmed at introduction in `afc0d35`, reason string literally says "pending showups must be deleted on stop" while deleting unconditionally) — but that alone is not the story. The test was *correctly* encoding PR #21's real, deliberate design; the design was reasonable *conditional on* the stats snapshot working. The actual regression is that the snapshot silently stopped working and nothing re-validated the conditional. HAB-208's fix sidesteps this entirely by no longer relying on a frozen snapshot — past/resolved showups just stay in the table.

**Proposed prevention (sized to the actual gap, not a blanket policy)**

The specific failure mode here is: a persistence-layer decision ("don't persist X") silently invalidates an assumption an *unrelated*, earlier feature depended on, with no test exercising the two together. A blanket "add more integration tests" policy is too broad. Narrower, actionable version: when a domain model gains a field that's computed/nulled at the mapper boundary (`fromRow` returning `null`/a default for something the constructor accepts), grep for other read sites of that field across the codebase before merging — `pact.stats` had exactly one real consumer (`currentStats`'s fallback branch) and a one-line grep at WU2 time would have surfaced it. This is cheap enough to be a review-checklist item, not a new test-infrastructure investment.

**Debrief agenda item: postmortem tickets should carry a self-nudging deadline**

User feedback mid-ticket: for this class of ticket (root-cause investigation, no urgency pressure, easy to let sit for weeks like HAB-209 did between 2026-07-31 creation and 2026-08-16 pickup), the agent should proactively set a deadline (e.g. Linear `dueDate`, ~1-2 weeks out) at ticket creation, and once it passes, ask the user at the next relevant touchpoint (session start / backlog summary) whether to proceed with the postmortem now or drop it. Raise at `/debrief`: should this become part of the (still-untrialed) `POSTMORTEM_WORKFLOW.md`, or a more general rule for any investigation-only ticket regardless of workflow?

**Debrief agenda item: prefer opening a Markdown/PR view over dumping long reports to the terminal**

User feedback: findings-heavy output (like this postmortem) is hard to read as raw terminal text. When a report is already being written to a Markdown file that ends up in a PR (as here), just open the PR (or the file) for the user instead of also pasting the full content into the chat response. Raise at `/debrief`: should this become a standing rule — e.g. auto-open the PR URL (or the artifact) whenever a substantial Markdown report is produced, rather than relying on the user to ask for it each time?

**Confirmed and broadened:** user liked opening the PR URL directly in the browser (`open <url>`, as done for PR #384) once it exists — and clarified this isn't specific to findings-heavy reports, it applies to **any** PR opened during a session, feature work included. Already adopted as a standing habit (not deferred to `/debrief`): `open <pr-url>` right after `gh pr create` succeeds, every time, mirroring how `summarize`'s `render_html_view` auto-opens `backlog.local.html` (see [[feedback_summarize_html_view]]). `/debrief` should still confirm this is documented in the relevant workflow step (`docs/FEATURE_WORKFLOW.md` step 9's "Inform the user of the PR URL") so it isn't only tribal knowledge in this note.

**Debrief agenda item (deferred, not yet actionable): GitHub's own Markdown rendering is still tiresome to read**

User follow-up: even with the PR auto-opened in the browser, GitHub's PR/Markdown rendering itself is tiresome to read for content like this. Explicitly flagged as "think about later," not something to solve now — no fix proposed yet. Raise at `/debrief` as an open question: is there a better delivery surface for findings-heavy reports than a GitHub PR diff view (e.g. an Artifact instead of/alongside the PR, a different PR body format, etc.)? Needs its own exploration, not a snap decision mid-ticket.

## Debrief summary

### 2026-08-16

**What went well**
- The ad hoc `POSTMORTEM_WORKFLOW.md` trial (origin tracing → reconstruct reasoning → assess detection gap → propose prevention) worked well enough to formalize as `docs/workflows/POSTMORTEM.md`. Tracing the true origin took three hops past two unrelated refactors to a three-month-old PR, and the actual regression turned out to be two individually-reasonable decisions in separate PRs whose interaction nothing tested — the proposed prevention (grep for other read sites when a mapper starts nulling/computing a field) is sized to that specific gap, not a blanket policy.

**What was hard or surprising**
- Reading a findings-heavy report as a wall of monotonous Markdown text in GitHub's PR view was hard on the eye, even after auto-opening the PR in the browser. No fix adopted yet — flagged for a dedicated research ticket instead of a snap decision here.

**What to change**
- Postmortem/investigation tickets should carry a due date so they don't silently sit for weeks (HAB-209 sat 16 days) — now step 2 of `docs/workflows/POSTMORTEM.md`, with `summarize` flagging overdue ones.
- Always `open` a PR's URL in the browser right after creating it — for any PR, not just report PRs. Now documented in `docs/workflows/FEATURE.md` step 9, not just tribal/memory knowledge.
- GitHub's own Markdown rendering being tiresome for long reports needs its own research ticket (via `/brief`) — not resolved here.
- Grouped the four workflow docs into `docs/workflows/` and dropped the `_WORKFLOW` suffix (`FEATURE.md`, `TROUBLESHOOT.md`, `RESEARCH.md`, `POSTMORTEM.md`) now that there are four of them.
