# HAB-173: Timeline screen title shows "Timeline" instead of "<Pact name> - Timeline" until data loads

## Notes

## Debrief summary

### 2026-07-16

**What went well**
- The bug was quickly identified, fixed, and pushed — a clean, contained fix (`initialHabitName` forwarded from Pact Details into the Timeline route) with no surprises during implementation.
- A tangential idea that came up mid-session (a shared stats+timeline cache) was correctly spun off into its own ticket via `/brief` instead of scope-creeping into this PR.

**What was hard or surprising**
- Nothing during implementation — straightforward start to finish.
- Process surprise found during debrief: `/brief` created HAB-175 as a duplicate of HAB-174, which already existed and already covered the same shared-cache idea (with richer context: the prior canceled HAB-126 attempt and its rejection reasons). Root cause: `PLAN.local.md` — a personal, gitignored scratch plan — already listed HAB-174 for this exact idea, but nothing in the `brief` or `summarize` skills reads that file, so the duplicate wasn't caught before ticket creation.

**What to change**
- Added a `PLAN.local.md`-survey step to `skills/manage/summarize/SKILL.md` (step 0): if the file exists and has content, cross-check its tickets against current Linear state, present the surviving plan, and ask whether to follow it, modify it, start a new plan alongside it, or proceed with the regular backlog.
- Added a duplicate-check step to `skills/design/brief/SKILL.md` (step 1): read `PLAN.local.md` if present, and search Linear for existing tickets covering similar ground, before opening the clarifying dialog.
- Confirmed with the user: interactive, turn-by-turn dialog skills (`debrief`, `brief`) should run inline in the main session rather than via a spawned subagent — the per-turn subagent close/reopen cycle was disruptive.
