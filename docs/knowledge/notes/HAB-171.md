# HAB-171: /checkup skill — periodic code-quality checkup procedure

## Notes

### 2026-07-15

**WU scope summary (from approved plan, Linear comment `551e9fa4`)**

Recorded at plan-approval time so a later session can resume WU2 without re-deriving scope from chat history. Two work units:

- **WU1 — checkup tracking-doc format + `due.py` + tests** (`feature/HAB-171-WU1-checkup-tracking`). Establishes the tracking-doc format under the new `docs/knowledge/checkups/` vault (one dated checkup record per file, each dimension carrying a next-due date). Adds `scripts/checkup/due.py` — a stdlib-only helper that reads the checkup records and reports which dimensions are due, plus its `unittest` test suite under `scripts/checkup/tests/`. No skill wiring yet; this WU is pure format + tooling so the skill in WU2 has something to read. CHANGELOG tag `[wip]`.

- **WU2 — `/checkup` skill + 8 heuristics + slash command + session-start wiring** (`feature/HAB-171-WU2-checkup-skill`). Adds the `skills/manage/checkup/` skill implementing the 8 non-mechanical quality heuristics, the thin `.claude/commands/checkup.md` slash-command stub, and the session-start wiring so due checkups surface at session start (consuming `due.py` from WU1). Makes the feature user-visible; final WU, so CHANGELOG tag `[meta]`.

Both WUs are meta/tooling only — no `lib/` production code, no app runtime surface. Effort/reasoning tier for the skill itself: THOROUGH / ARCHITECTURAL (see `docs/MODEL_TIERS.md` meta-skills map).

**Debrief candidate: context compaction between tickets/WUs isn't systematic**

Across WU1 of this ticket, context grew large enough to require a manual `/compact` mid-session, and the standing preference ([[feedback_context_compaction]]) to remind after each commit is easy to apply inconsistently since it depends on the orchestrator remembering to say it every time. Worth discussing at debrief: whether the workflow should build in an explicit checkpoint (e.g. a step in FEATURE_WORKFLOW.md's multi-WU cycle, or a hook) that forces/reminds context compaction between WUs of the same ticket and between tickets, rather than relying on ad hoc reminders.

**Debrief candidate: synchronize HabitLoop and YAB**

User flagged on WU2 PR approval: "we will need to synchronize HabitLoop and YAB." Raised as a note for the debrief dialog — no further detail given yet; surface it as an open question rather than assuming scope.

## Debrief summary

### 2026-07-16

**What went well**
- Implementation went smoothly overall — no unexpected friction or missing context.
- WU1/WU2 split anticipated the eventual scope well; final size was larger than expected but the split held up.

**What was hard or surprising**
- Nothing beyond the overall size of the work turning out larger than anticipated.

**What to change**
- Context compaction between WUs/tickets was ad hoc, relying on a verbal reminder that's easy to skip. Since Claude cannot trigger `/compact` autonomously, tightened `docs/FEATURE_WORKFLOW.md` to distinguish two explicit checkpoints: **compact** mid-ticket (after each commit — step 11; and as a hard checkpoint between WUs of the same ticket — multi-WU cycle step 3), and **clear** at the ticket boundary (step 14, now explicit: "before starting a new ticket").
- YAB (Yuriys-agentic-boyz) is a sibling project serving as a template ground for other projects; when HabitLoop's harness (skills/workflow docs) diverges from YAB, that improvement should flow back to keep YAB in sync. No in-repo action from this ticket — user is opening a tracking item in YAB directly, outside this debrief's scope.
