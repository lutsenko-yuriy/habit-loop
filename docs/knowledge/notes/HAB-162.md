# HAB-162: Notify/speak when a Claude Code session needs input or stops

## Notes

<!-- Each entry: dated heading, then named observations (bold title + paragraph).
     If the ticket is reopened after this file is finalised, create HAB-XX-adjust-N.md instead of editing this file. -->

## Debrief summary

### 2026-07-12

**What went well**
- Immediate real-world testability: since this ticket shipped a local script rather than app code, every change could be smoke-tested on the spot (real `osascript` notification + `say` speech, live focus-switching to verify the suppression heuristic) instead of waiting on a build/deploy cycle.

**What was hard or surprising**
- Nothing structural — the only surprise was the default `say` voice being funnier than expected.

**What to change**
- Nothing — process worked as-is for this ticket.
