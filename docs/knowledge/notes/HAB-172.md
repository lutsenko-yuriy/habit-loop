---
bookmarks: [cross-project-sync]
---

# HAB-172: Synchronize HabitLoop harness improvements into YAB

## Notes

### 2026-09-09

**Cross-repo ticket — all the actual work landed in YAB, not this repo**

This ticket's deliverable is a PR against the sibling `yuriys-agentic-boyz` (YAB) repo, not against `habit_loop`: [PR #12](https://github.com/lutsenko-yuriy/yuriys-agentic-boyz/pull/12), branch `feature/HAB-172-sync-harness`. No `habit_loop` code or docs changed as part of this ticket itself (this note is the only exception).

**Scope was expanded mid-ticket** to include packaging the harness as an installable Claude Code plugin/marketplace — the user wants to publish it so other Claude Code users can fetch and use it, after validating it on two real projects first (a from-scratch one and a retrofit). This was split into two new, explicitly-sequenced follow-up tickets rather than folded into HAB-172's own charter:
- **HAB-274** — Retrofit `mordovorot` (an existing repo) to use YAB's harness, once this ticket's port lands.
- **HAB-275** — Package YAB as a Claude Code plugin and publish a self-hosted marketplace, blocked on HAB-172 + HAB-165 (CheckLister, the from-scratch validation ticket) + HAB-274 (the retrofit validation ticket) all landing first.

**Ported to YAB** (genericized — `HAB-XX` → `{{ISSUE_PREFIX}}-XX`, Flutter/Firebase specifics stripped or made conditional):
- `docs/workflows/{FEATURE,RESEARCH,TROUBLESHOOT,POSTMORTEM,MULTI_WU}.md` — full workflow split, replacing YAB's single inline `AGENTS.md` workflow section
- `docs/knowledge/decisions/` — the ADR system (README, TEMPLATE, plus ADR-0001 for the two-tier checkup and ADR-0002 for the dialectical research methodology, both ported as YAB's own first decisions)
- `skills/manage/checkup/` + `scripts/checkup/due.py` — the two-tier periodic code-quality checkup, with its test suite (17 tests, all passing in YAB)
- `skills/design/research/` — the thesis/antithesis/synthesis literature-research skill
- `docs/knowledge/notes/` bookmarks system — `BOOKMARKS.md` (seeded empty) + `scripts/notes/index.py` tagged-index generator, with its test suite (36 tests, all passing in YAB)
- `skills/shared/decision-guidelines.md` (new to YAB) and `dialog-principles.md` (updated with sections YAB's copy predated: staying on topic, option-space breadth, question format)
- `AGENTS.md` doc/skill tables and session-start checklist, updated to reference all of the above

**Deliberately deferred, not ported this pass**: `.claude/hooks/` (`nightly_block.sh`, `break_reminder.sh`, `alert.sh`, `session_start.sh`) — valuable session-quality patterns, but tightly coupled to a hardcoded absolute project path and Linear-specific wording in their current form. Genericizing them properly felt like its own piece of work rather than something to rush inside an already-large port; flagged here as a real gap rather than silently skipped.

**Verification note**: ported Python scripts (`due.py`, `index.py`) were both unit-tested and exercised live against fresh YAB state (`python3.12 scripts/checkup/due.py`, `scripts/notes/index.py --check`) before committing — not just copied and assumed correct.
