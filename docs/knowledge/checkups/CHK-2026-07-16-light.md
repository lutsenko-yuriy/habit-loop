# CHK-2026-07-16-light: first light checkup — glossary gaps, one doc-drift, one dangling scenario stub

## Run info

- **Tier:** light
- **Date:** 2026-07-16
- **Period covered:** 2026-07
- **Dimensions walked:** unused functionality, scenario quality, glossary/naming drift, doc-reality drift, feature-flag lifecycle

## Debt quadrant legend

Every finding below is classified using [Fowler's Technical Debt Quadrant](https://martinfowler.com/bliki/TechnicalDebtQuadrant.html) — deliberate vs. inadvertent × reckless vs. prudent:

| Quadrant | Meaning | Typical deadline urgency |
|---|---|---|
| Reckless-deliberate | Known-bad code, shipped under pressure on purpose | Immediate |
| Reckless-inadvertent | A mistake, now understood | Soon |
| Prudent-deliberate | A conscious trade-off with a known payoff date | Planned |
| Prudent-inadvertent | A design flaw only visible in hindsight — most checkup findings land here | Normal (set by judgement) |

## Findings

### Unused functionality

#### Dead-code-check orphans — false positives, no action needed

- **Finding:** `/dead-code-check` flagged 3 informational orphans (`test_runner.dart`, `language_change_flow_test.dart`, `onboarding_carousel_flow_test.dart`) — all confirmed false positives. `test_runner.dart` is the combined on-device entry point (imports every suite, so nothing imports it); the other two are imported *by* `test_runner.dart` under aliases, but only via a relative import (no `package:habit_loop` import of their own), which is exactly the detector's known blind spot for harness-based test files.
- **Debt quadrant:** n/a — no actionable debt.
- **Disposition:** no action needed.
- **Graduation candidate:** no.

### Scenario quality

#### Stale commented-out TODO — fixed inline

- **Finding:** `integration_test/reminder_locale_flow_test.dart:34` had a stale commented-out line — `// TODO: restore late AppHarness h; tearDown(() => h.dispose());` — left over from a prior refactor to the per-test `addTearDown(h.dispose)` pattern (which all 3 tests in the file already use correctly). Dead comment, no behavioural gap.
- **Debt quadrant:** prudent-inadvertent.
- **Disposition:** fixed inline (commit pending — this run's commit).
- **Graduation candidate:** no.

#### Dangling "Load more" pagination test stub (`CHK-2026-07-16-light-1`)

- **Finding:** `integration_test/pact_timeline_flow_test.dart:406-422` ("Pact timeline — pagination" group, `load_more_reveals_older_events` test) is a scenario stub with 6 unfilled `// TODO:` steps and no driver code — the sole remaining trace of a "Load more" pagination control. See the combined write-up under Doc-reality drift below; same root cause, one deferred finding covers both.
- **Debt quadrant:** prudent-inadvertent.
- **Disposition:** deferred — see ledger, deadline `2026-08-15`.
- **Graduation candidate:** no.

### Glossary/naming drift

#### Missing glossary entries: Redeem, Milestone, Pact timeline — fixed inline

- **Finding:** Three real domain/code terms had no `docs/GLOSSARY.md` entry: **Redeem (a showup)** (`showup_redemption_enabled`, `ShowupRedeemedEvent`), **Milestone** (`PactTimelineMilestone` and its 7 subtypes — explicitly named in code comments to avoid confusion with analytics events, yet absent from the one doc meant to prevent exactly that confusion), and **Pact timeline** itself (the HAB-116 feature has no Core-concepts/UI-surfaces definition at all, despite having per-language translations in the glossary's language table).
- **Debt quadrant:** prudent-inadvertent — normal vocabulary lag as features ship faster than glossary upkeep.
- **Disposition:** fixed inline (commit pending — this run's commit).
- **Graduation candidate:** no.

### Doc-reality drift

#### Missing analytics event doc entry — fixed inline

- **Finding:** `docs/ANALYTICS_EVENTS.md` was missing the `notifications_scheduled` event (`NotificationsScheduledEvent`, `lib/slices/reminder/analytics/reminder_analytics_events.dart`) — fired on every reminder-scheduling pass, but never documented. Cross-checked all 34 event classes against the doc by their `name` string (not class name, which is how the doc is organised) — this was the only real gap.
- **Debt quadrant:** prudent-inadvertent.
- **Disposition:** fixed inline (commit pending — this run's commit).
- **Graduation candidate:** yes — a script diffing `AnalyticsEvent.name` strings in `lib/` against `docs/ANALYTICS_EVENTS.md` `### \`event_name\`` headings would catch this mechanically; candidate for `scripts/dead_code/` or a new `scripts/analytics_docs_check.py`.

#### Stale paginated-design description in ARCHITECTURE.md (`CHK-2026-07-16-light-1`)

- **Finding:** `docs/ARCHITECTURE.md`'s `PactTimelineConfig`/`PactTimelineService`/`PactTimelinePage` descriptions documented a paginated design (`loadPage(pactId, pageNumber)`, a `hasMore` flag, `first_page_size`/`nth_page_size` config) that does not exist in the shipped code. The actual API is `PactTimelineService.loadAll({required pactId, now})` → `PactTimelinePage` with no pagination fields at all — confirmed by reading the service, the page value object, the view model (which calls `loadAll` directly, no "load more" trigger anywhere), and `PactTimelineConfig` (only `enabled`, `milestoneGroupingThreshold`, `noGroupingTailPeriodInDays`). `docs/CHANGELOG.md:606` confirms this was a deliberate removal ("pagination is not a service-layer concern"), matching the `docs/knowledge/notes/HAB-116.md` debrief note that pagination was redesigned away mid-ticket. `docs/PRODUCT_SPEC.md` was already accurate (describes the timeline with no pagination language) — only `ARCHITECTURE.md` had drifted, alongside the dangling test stub above and the glossary's own now-corrected "Pact timeline" entry (also said "paginated" until this run).
- **Debt quadrant:** prudent-inadvertent — the doc simply wasn't updated when the mid-ticket redesign happened.
- **Disposition:** `docs/ARCHITECTURE.md` fixed inline (commit pending — this run's commit). The dangling test stub itself is a product decision (implement "Load more" for real, or delete the stub and close the loop) — deferred, see ledger, deadline `2026-08-15`. Same ledger ID as the scenario-quality finding above — one product question, two symptoms.
- **Graduation candidate:** no.

### Feature-flag lifecycle

#### No "Review by" tracking on feature toggles — fixed inline

- **Finding:** `docs/FEATURE_TOGGLES.md` had no `Review by` tracking at all for any of its 5 flags. All 5 are young (introduced 2026-06-14 through 2026-07-05, per `git log -S`), so none are stale-removal candidates yet — this run establishes the baseline.
- **Debt quadrant:** prudent-inadvertent.
- **Disposition:** fixed inline — added a `Review by` column: `language_selection_enabled` and `network_sync_enabled` (structural/operational toggles, longer horizon) → `2027-01-16`; `pact_timeline_enabled`, `showup_redemption_enabled`, `about_screen_enabled` (feature-launch kill-switches, shorter horizon so removal-once-stable gets considered sooner) → `2026-11-16`.
- **Graduation candidate:** no.

## Ledger updates

- Cadence & due status: `Light` → Last run `2026-07-16`, Period covered `2026-07`
- Open findings added: `CHK-2026-07-16-light-1`
- Resolved findings closed: none
